# Ex. 4.6 Odd-numbered letters
# Using the third argument of the range() function, create a list of odd-numbers from 1 to 20,
# and then display them using the for loop.

values = list(range(1, 20, 2))

for value in values:
    print(value)
